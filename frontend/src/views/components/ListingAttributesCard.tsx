import type { Attribute, AttributeData, ListingImage } from "../../types";
import { ListingImageCarousel } from "./ListingImageCarousel";

type Props = {
  attributes: Attribute[];
  attributesData: AttributeData[];
}
  

export const ListingAttributesCard = ({ attributes, attributesData }: Props) => {
  
 let formattedAttributes = {
    
 }

  return (
    <div>
        {}
    </div>

      
  );
};
